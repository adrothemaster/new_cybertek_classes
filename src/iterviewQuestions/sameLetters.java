package iterviewQuestions;

public class sameLetters {
    public static void main(String[] args) {
        String a = "fork";
        String b = "korf";

        String tempA = "";
        String tempB = "";

        for (int i = 0; i < a.length(); i++) {

            if (tempA.contains(a.charAt(i) + "")){

            }
        }
        for (int j = 0; j < b.length(); j++) {

            if (tempB.contains(b.charAt(j)+"")){

            }

        }

    }
}
